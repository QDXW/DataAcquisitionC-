#include "GPIO.h"
#include "chip.h"



/*
void GPIO_SetDir(uint8_t port,uint8_t pin,bool dir)
{
    Chip_GPIO_WriteDirBit(LPC_GPIO, port, pin, dir);
}

void GPIO_SetBit(uint8_t port,uint8_t pin,bool val)
{
    Chip_GPIO_WritePortBit(LPC_GPIO,port,pin,val);
}
*/
